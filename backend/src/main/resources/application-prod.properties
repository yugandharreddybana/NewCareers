# Production — secrets from platform env injection and/or AWS Secrets Manager.
# Do not commit secret values here.
spring.cloud.aws.secretsmanager.enabled=true
spring.config.import=optional:aws-secretsmanager:${AWS_SECRETS_MANAGER_NAME:careerops/prod}
auth.login.word-captcha.required=true
