# Public only — no secrets. CI injects real values at build time.
# Middleware BFF URL — never point at Java :8080 directly.
VITE_API_URL=https://api.careerops.com
VITE_RECAPTCHA_SITE_KEY=
VITE_GOOGLE_CLIENT_ID=
VITE_STRIPE_PUBLIC_KEY=pk_live_
