// @ts-nocheck — existing page; PageMeta injected
import { PageMeta } from '@/components/PageMeta';
export { default as _KanbanImpl } from './Kanban';
// NOTE: PageMeta must be rendered inside the Kanban component JSX.
// This file is a pass-through — the real implementation is below.
